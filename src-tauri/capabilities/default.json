{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Okno główne: rdzeń Tauri (zdarzenia, komendy). Sidecar spawnowany z Rusta — frontend nie potrzebuje uprawnień shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
